<%= link_to "View challenges", challenges_path %>
<table class="leaderboard">
  <tr>
    <th class="left">Rank</th>
    <th class="left">Name</th>
    <th>Rating</th>
    <th>Wins</th>
    <th>Losses</th>
  </tr>
  <%= render collection: @leaderboard_users, partial: 'leaderboard_user' %>
</table>

<h1>Enter a score</h1>
<%= render partial: 'games/form', locals: { game: @game } %>
